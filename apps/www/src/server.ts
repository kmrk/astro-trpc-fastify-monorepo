// import  startServer  from '@astrojs/node';
// import { handler } from '../dist/server/entry.mjs'; // 指向打包后的文件
//
// // 启动服务器，监听端口
// startServer(handler, {
//   port: 3000,
// }).then(() => {
//   console.log('Server started on http://localhost:3000');
// }).catch((err: Error) => {
//   console.error('Failed to start server:', err);
// });
